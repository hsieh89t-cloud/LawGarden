<%*
const today = tp.date.now("YYYY-MM-DD");
const ts = tp.date.now("YYYYMMDD_HHmm");
let topic = await tp.system.prompt("主題（例如：GAS 讀寫 Spreadsheet）", "");
topic = topic ? topic.trim() : "";
const area = await tp.system.suggester(
  ["GAS", "PWA", "API/金流", "其他"],
  ["GAS", "PWA", "API/金流", "其他"]
);
let clip = "";
try { clip = await tp.system.clipboard(); } catch (e) { clip = ""; }
if (!clip || !clip.trim()) clip = "（請在此貼上原文／英文說明／錯誤訊息）";

const fileTitle = topic ? topic : `程式收件_${area}_${ts}`;
await tp.file.rename(fileTitle);
%>
---
建立日期: "<%= today %>"
狀態: "未整理"
類型: "程式"
領域: "<%= area %>"
標籤: []
---

# <%= fileTitle %>

原文或貼上的資料
<%= clip %>

中文白話翻譯
{{把原文丟給 GPT，請它翻成白話中文，再貼回來}}

我理解的版本
{{用你自己的話講一次，越口語越好}}

最小可跑範例
```js
// 只放最小可跑版本（能跑最重要）
```

踩坑紀錄
{{錯誤訊息＋原因＋怎麼解}}

下一步
- [ ] 把範例整理成可重複使用的小模組
- [ ] 補上測試方式與輸入輸出
