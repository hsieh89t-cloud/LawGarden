<%*
const today = tp.date.now("YYYY-MM-DD");
const lawName = await tp.system.prompt("法律名稱（例如：民法）", "");
const articleNo = await tp.system.prompt("條文編號（例如：184 或 185-3）", "");
let clip = "";
try {
  clip = await tp.system.clipboard();
} catch (e) {
  clip = "";
}
if (lawName && articleNo) await tp.file.rename(`${lawName}第${articleNo}條`);
%>
---
建立日期: "<%= today %>"
編輯狀態: "待審核"
法律位階: "法律"
核心領域: [""]
條文關鍵字: ["", ""]
判決連結: []
釋字連結: []
---

# <%= lawName %>第<%= articleNo %>條

核心摘要
（一句話講清楚本條用途）

條文原文
<% if (clip && clip.trim()) { %>
<%= clip %>
<% } else { %>
（請在此貼上法條原文）
<% } %>

你自己的白話版
（用你自己的話講一次）

適用情境
（什麼狀況會用到它）

要件與效果
（最重要的 3–6 句：要件怎麼判、效果是什麼）

常見爭點
（實務最常吵哪裡）

權威來源
法條連結：
法規沿革：
