<%*
const lawName = await tp.system.prompt("法律名稱", "民法");
const articleNo = await tp.system.prompt("條文編號", "184");
const domainRaw = await tp.system.prompt("核心領域（例如：民法/侵權；可留空）", "");
const kw1Raw = await tp.system.prompt("條文關鍵字1（可留空）", "");
const kw2Raw = await tp.system.prompt("條文關鍵字2（可留空）", "");
const editor = await tp.system.prompt("負責編輯（可留空）", "");

const today = tp.date.now("YYYY-MM-DD");

const normTag = (s) => {
  const t = (((s) !== null && (s) !== undefined ? (s) : "").trim());
  if (!t) return "";
  return "#" + t.replace(/^#+/, "");
};

const domainTag = normTag(domainRaw);
const kw1Tag = normTag(kw1Raw);
const kw2Tag = normTag(kw2Raw);

// 若你想插入模板後自動把檔名改成「民法第184條」，取消下一行註解即可
// await tp.file.rename(`${lawName}第${articleNo}條`);
%>
---
條文狀態: ""
編輯狀態: "待審核"
法律位階: "法律"
核心領域:
<%* if (domainTag) { %>
  - "<%= domainTag %>"
<%* } else { %>
  - ""
<%* } %>
條文關鍵字:
<%* if (kw1Tag) { %>
  - "<%= kw1Tag %>"
<%* } else { %>
  - ""
<%* } %>
<%* if (kw2Tag) { %>
  - "<%= kw2Tag %>"
<%* } %>
重要性: 0
備考星等: "★"
修正日期: ""
最後更新: "<%= today %>"
負責編輯: "<%= editor %>"
判決連結: []
釋字連結: []
---

# <%= lawName %>第<%= articleNo %>條

核心摘要  
{{一句話講清楚本條的重點與用途}}

快速導航  
本條核心在於 =={{一句話概括}}==。主要用在 {{典型情境}}，用來解決 {{主要問題}}。

條文原文  
{{法條原文}}

立法意旨與體系位置  
本條的目的在於 =={{保護法益／填補漏洞／政策目的}}==，回應 {{具體問題}}。在體系上，與 [[{{同法第XX條}}]]、[[{{另法第YY條}}]] 常一起搭配或形成競合。若需要對照，與 {{近似概念}} 的差異在於 =={{差異點}}==。上位原則可連到 [[{{憲法原則或基本法理}}]]。

要件與效果  
本條適用時，通常先看行為與客體，再看是否出現損害或法定結果，並確認因果關係成立。接著評價違法性或不法性，最後判斷主觀歸責。要件該當時，法律效果是 {{主要效果}}，其範圍與限制是 {{期間／範圍／減免責要件}}。

核心概念  
第一個要釐清的概念是「{{概念名稱1}}」。它的定義是 {{一句話定義}}，判斷通常抓 {{判斷要點}}，常見誤區是 {{常見混淆點}}。第二個概念是「{{概念名稱2}}」。它的定義是 {{一句話定義}}，判斷重點是 {{判斷要點}}，實務趨勢是 {{實務操作方式}}。

關鍵裁判  
爭點是 {{一句話爭點}}。要旨是 {{一句話要旨}}。標籤 `#{{標籤1}}` `#{{標籤2}}`  
爭點是 {{一句話爭點}}。要旨是 {{一句話要旨}}。標籤 `#{{標籤1}}` `#{{標籤2}}`

常見爭點與攻防  
主張方通常會把重點放在 {{常主張的要件與舉證}}，抗辯方多從 {{常抗辯的要件或阻卻事由}} 下手。裁判實務最常卡在 {{法院常採的關鍵判斷點}}。

實務用法  
建議順序是：先定性本條規範類型，再抓客體與保護目的，逐一涵攝要件，最後處理競合與效果。你要強調的亮點通常是 {{你希望提醒自己的亮點}}。

編輯備註  
條文狀態與修正日期查證：{{查證結果或留空原因}}。判決要旨覆核：{{已覆核／待覆核}}。待補內容：{{待補}}。

權威來源  
法條連結：{{URL}}  
法規沿革：{{URL}}  
判決原文：{{URL}}
