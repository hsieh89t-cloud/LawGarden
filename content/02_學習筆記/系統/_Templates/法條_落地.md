<%*
const today = tp.date.now("YYYY-MM-DD");
const lawName = (await tp.system.prompt("法律名稱（例如：民法）", "民法") || "").trim() || "民法";
const articleNo = (await tp.system.prompt("條文編號（例如：184 或 185-3）", "184") || "").trim() || "184";
let raw = "";
try { raw = await tp.system.clipboard(); } catch (e) { raw = ""; }
if (!raw || !raw.trim()) raw = "（請在此貼上法條原文）";

await tp.file.rename(`${lawName}第${articleNo}條`);
%>
---
建立日期: "<%= today %>"
編輯狀態: "待審核"
類型: "法條"
法律名稱: "<%= lawName %>"
條文編號: "<%= articleNo %>"
標籤: []
---

# <%= lawName %>第<%= articleNo %>條

核心摘要
{{一句話講清楚本條用途}}

條文原文
<%= raw %>

要件與效果
{{用自己的話拆成 3–6 句}}

關聯判決／釋字
- [[ ]]

待查／待補
{{權威來源連結、沿革、關聯條文}}
