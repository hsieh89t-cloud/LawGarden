<%*
const today = tp.date.now("YYYY-MM-DD");
const ts = tp.date.now("YYYYMMDD_HHmm");
const promptResult = await tp.system.prompt("標題（可空白）", "");
const title = (promptResult && typeof promptResult === "string") ? promptResult.trim() : "";
let kind = await tp.system.suggester(
  ["法條", "判決", "議題", "程式", "其他"],
  ["法條", "判決", "議題", "程式", "其他"]
);
if (!kind) kind = "其他";
let clip = "";
try {
  clip = await tp.system.clipboard();
} catch (e) {
  clip = "";
}
if (!clip || !clip.trim()) clip = "（請在此貼上原文）";
const fileTitle = title ? title : ("收件_" + kind + "_" + ts);
function safeName(s) {
  if (!s) return "";
  return s.replace(/[\\/:*?"<>|]/g, "－").trim();
}
const newName = safeName(fileTitle) || ("未命名_" + tp.date.now("YYYYMMDD_HHmm"));
try {
  await tp.file.rename(newName);
} catch (e) { }
-%>---
建立日期: "<%= today %>"
狀態: "未整理"
類型: "<%= kind %>"
標籤: []
---

# <%= fileTitle %>

## 原文
<%= clip %>

## 我先懂了什麼
（用口語寫兩三句就好）

## 待查／待補
（需要補來源、補案號、補法條或補例子就寫這裡）

## 下一步
- [ ] 用 GPT 整理成可讀版本（摘要＋重點＋我的觀點）
- [ ] 歸檔到：法條庫／判決庫／議題庫／文章庫／程式學習
