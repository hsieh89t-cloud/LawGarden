<%*
const today = tp.date.now("YYYY-MM-DD");

let court = await tp.system.prompt("法院（例如：最高法院／高等法院）", "最高法院");
if (!court) court = "最高法院";

let caseNo = await tp.system.prompt("案號（例如：100台上328）", "");
if (caseNo == null) caseNo = "";

let issue = await tp.system.prompt("一句話爭點（可留空）", "");
if (issue == null) issue = "";

let sourceUrl = await tp.system.prompt("判決書連結（可留空）", "");
if (sourceUrl == null) sourceUrl = "";

let clip = "";
try { clip = await tp.system.clipboard(); } catch (e) { clip = ""; }

const title = caseNo ? `${court}${caseNo}` : `${court}判決_${tp.date.now("YYYYMMDD_HHmm")}`;

const safeName = (s) => (s ?? "")
  .replace(/[\\\/:*?"<>|]/g, "－")
  .replace(/\s+/g, " ")
  .trim();

const newName = safeName(title) || `未命名_${tp.date.now("YYYYMMDD_HHmm")}`;
try { await tp.file.rename(newName); } catch (e) { /* ignore */ }
-%>
***
建立日期: "<%= today %>"
編輯狀態: "待審核"
類型: "判決"
法院: "<%= court %>"
案號: "<%= caseNo %>"
爭點一句話: "<%= issue %>"
標籤: []
來源連結: "<%= sourceUrl %>"
***

# <%= title %>

## 爭點
<%= issue ? issue : "（一句話寫：本案在吵什麼）" %>

## 要旨
（用 3–8 句，把法院怎麼判講清楚）

## 原文摘錄
<% if (clip && clip.trim()) { -%>
<%= "```text\n" + clip.trim() + "\n```" %>
<% } else { -%>
（請在此貼上判決摘錄／要旨／你覺得最重要的段落）
<% } -%>

## 你自己的白話版
（把要旨用你自己的話講一次）

## 可用在什麼地方
（哪一類題目／書狀／申論可以引用它）

## 來源
- 判決書連結：<%= sourceUrl ? sourceUrl : "（補上連結）" %>

***



