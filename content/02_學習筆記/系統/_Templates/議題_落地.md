<%*
const today = tp.date.now("YYYY-MM-DD");
const ts = tp.date.now("YYYYMMDD_HHmm");
let topic = await tp.system.prompt("議題名稱（例如：強制住院正當程序）", "");
topic = topic ? topic.trim() : "";
let clip = "";
try { clip = await tp.system.clipboard(); } catch (e) { clip = ""; }
if (!clip || !clip.trim()) clip = "（可貼上題目、你的口述逐字稿、或相關資料）";

const fileTitle = topic ? topic : `議題_${ts}`;
await tp.file.rename(fileTitle);
%>
---
建立日期: "<%= today %>"
狀態: "草稿"
類型: "議題"
標籤: []
---

# <%= fileTitle %>

問題／原始資料
<%= clip %>

核心爭點
{{一到兩句}}

法律基礎
- [[ ]] 

實務／判例
- [[ ]] 

我的觀點
{{你自己的看法，先寫得出來最重要}}

待查／待補
{{要補的法條、判決、資料來源}}
